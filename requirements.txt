# Install a CUDA-enabled PyTorch build appropriate for the machine when the
# default PyPI wheel does not match the installed CUDA driver.
torch==2.4.1
torchvision==0.19.1

# Stable Diffusion x4 upscaler, CLIP reward, and LoRA training.
diffusers==0.30.3
transformers==4.44.2
accelerate==0.34.2
peft==0.12.0
safetensors==0.4.5
ftfy==6.2.3

# Image loading, reward metrics, and numerical operations.
Pillow==10.4.0
opencv-python-headless==4.10.0.84
numpy==1.24.4

# Used by the exploratory upscaling notebook.
matplotlib==3.7.5
ipykernel==6.29.5
nbformat==5.10.4
nbclient==0.10.0
